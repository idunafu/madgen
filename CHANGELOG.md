<!-- 書き方は CONTRIBUTING.md の「変更履歴」を参照すること。 -->

# 変更履歴

## [Unreleased]

### 追加

- 打楽器トラック（GM のチャンネル10）を、音素の素材から作るようにした（[#13]）。楽器ごとに音素を割り当て、同じ楽器には同じ音、別の楽器には別の音を使う。`--percussion pitched` で以前の動作に戻せる。
- トラックごとのフィルタ・EQ を追加した（[#14]）。`--filter Bass=lp:800` のように指定する。
- 打楽器の楽器ごとに使う音素を指定できるようにした（[#13]）。`--drum-material キック=b,g` のように、楽器名か GM のノート番号で指定する。

### 変更

- 打楽器の音量が上がった（[#13]）。従来は他の伴奏より10dB ほど小さかったが、同程度になった。
- 打楽器が素材を取り合う順番を、曲での登場順ではなく楽器の重要度順に固定した（[#13]）。従来は先に鳴った楽器がよい素材を取っていたため、同じ曲でもイントロの楽器やトラックの分割の仕方でキットが変わっていた。
- どの素材がどの楽器に割り当てられたかを、レンダリング時に表示するようにした（[#13]）。
- `--tracks` / `--ust-tracks` に当てはまるトラックが無いとき、そのファイルに入っているトラックの一覧（名前、音数、打楽器かどうか）を表示するようにした。別の曲のトラック名を指定してしまったときに、何を指定すればよいかが分かる。

### 修正

- Windows で日本語のファイル名を含む素材の解析が文字コードエラーで停止する問題を修正。ffmpeg の出力を UTF-8 として読み取るようにした。

## [0.2.0] - 2026-09-18

### 追加

- 動画をレイヤー合成で作るようにした（[#1]）。打楽器を背景に全画面で敷き、その他の伴奏を小さいパネルとして並べ、歌を中央に置く。`--video-layout fullscreen` で以前の全画面切り替えに戻せる。
- 何も映っていない部分をキー色で塗るようにした（[#1]）。他の編集ソフトでそのまま合成素材として使える。キー色は素材に最も含まれない色を自動で選ぶ。
- 伴奏だけ・歌だけの音声と動画（`accompaniment` / `vocals`）を出力するようにした（[#1]）。
- 音量調節を追加した（[#1]）。歌と伴奏の音量差を自動でそろえるほか、`--gain` でトラックごとに指定できる。
- 歌詞モード（UST / USTX）を追加した（[#1]）。音素の一致を最優先し、次にピッチと長さ、最後に接続の自然さで素材を選ぶ。
- 音素解析の実行場所とモデルを選べるようにした（[#2]）。`--device` と `--whisper-model` を追加。CPU でも動かせる。
- Windows 版 zip と Linux 版の単体バイナリを配布するようにした（[#4], [#6]）。Python を導入せずに使える。
- `madgen --version` を追加した（[#4]）。

### 変更

- 歌と伴奏の音量差の既定値を 6dB から 2dB にした（[#1]）。伴奏が聞こえにくかったため。
- 歌詞モードで、母音の芯を取り出して音符の長さに合わせるようにした（[#1]）。素材の区間には減衰や無音が含まれることが多く、そのままでは伸ばしきれなかった。

### 修正

- 同じ素材の同じ位置が続けて選ばれたとき、映像が止まって見えるのを直した（[#1]）。頭に戻さず再生を続けるようにした。
- 優先トラックが休んでいる間、音が鳴っていても黒画面になるのを直した（[#1]）。
- Windows のコンソールで、ヘルプに含まれる日本語が出力できずに落ちるのを直した（[#4]）。

## [0.1.0] - 2026-09-17

### 追加

- 最初の公開。メロディ（MIDI）に合わせて素材を切り貼りし、音声と動画を生成する。

[Unreleased]: https://github.com/0266st/madgen/compare/v0.2.0...HEAD
[0.2.0]: https://github.com/0266st/madgen/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/0266st/madgen/releases/tag/v0.1.0
[#13]: https://github.com/0266st/madgen/issues/13
[#14]: https://github.com/0266st/madgen/issues/14
[#1]: https://github.com/0266st/madgen/pull/1
[#2]: https://github.com/0266st/madgen/pull/2
[#4]: https://github.com/0266st/madgen/pull/4
[#6]: https://github.com/0266st/madgen/pull/6
